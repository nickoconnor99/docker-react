name: Deploy Frontend
on:
  push:
    branches:
      - "master"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker login -u ${{ secrets.paulallen99 }} -p ${{ secrets.dckr_pat_6OEzYNriLd0sil2LL8nxhEw6ZXo }}
      - run: docker build -t paulallen99/react-test -f Dockerfile.dev .
      - run: docker run -e CI=true paulallen99/react-test npm test
